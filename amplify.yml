version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20.19.0
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/smart-receipts/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
